{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "answer": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "The answer to the question, which is a list of elements in the correct order"
        }
    },
    "required": ["answer"],
    "additionalProperties": false
}